[package]
name = "epix-fs"
version = "0.1.0"
edition = "2021"
description = "Small platform filesystem primitives with explicit durability semantics."

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Storage_FileSystem"] }
