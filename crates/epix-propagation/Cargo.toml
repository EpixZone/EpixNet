[package]
name = "epix-propagation"
version = "0.1.0"
edition = "2021"
description = "In-process store-and-forward propagation of xite update notifications, served over the EDX control plane."

[dependencies]
tokio = { version = "1", features = ["sync"] }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
