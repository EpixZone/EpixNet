org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
# The Rust core is prebuilt into jniLibs by cargo-ndk (see shells/README.md);
# no cmake/ndk-build step runs in this project.

# Retain the explicit Kotlin 2.3 compiler during the AGP 9 migration.
android.builtInKotlin=false
android.newDsl=false
